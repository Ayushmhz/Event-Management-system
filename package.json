{
  "name": "college-event-management-root",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "postinstall": "npm install --prefix backend"
  }
}